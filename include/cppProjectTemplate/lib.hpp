#pragma once

#include <string>

/// \brief This is just a template of the function in the library
/// 
/// This returns a string from a library
///
std::string helloFromLibrary(const std::string& name);